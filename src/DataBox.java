//holds any data
/*public class DataBox <E> //means generic data//
{
    //for generics you can use E for elements, T for type, K for key and V for value
    private E obj;

    public  E getIt(){
       if (obj instanceof Integer){
           System.out.println("Is an Integar!!");
       }
            return obj;

    }
    public void addIt(E n){
        if (position>=obj.length){
            //size=size*2;
            String[] newNames=new String[((names).length*2)];
            for (int i=0;i<names.length;i++){
                newNames[i]=names[i];
            }
            newNames[position]=n;
            position++;
            names=new String[newNames.length];
            for (int i=0;i<newNames.length;i++){
                names[i]=newNames[i];
            }
        }
        else{
            names[position]=n;
            position++;
        }
        size++;
    bj=n;

    }
}*/
